export class User {

    constructor(id, login, email, password, image) {
        this.id = id;
        this.login = login;
        this.email = email;
        this.password = password;
        this.image = image;
    }
}